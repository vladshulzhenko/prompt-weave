---
"prompt-weave": minor
---

Add new prompt composition helpers: `Role`, `User`, `Assistant`, `Tool`, `Join`, and `Template`.

Refactor `System` to use the generic role helper internally, add runtime/type test coverage for new APIs, and expand README with short primary usage and additional real-world examples.
